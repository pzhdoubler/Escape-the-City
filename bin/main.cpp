#include <SFML/Graphics.hpp>

#include <LogicView.h>
#include <ScreenView.h>
#include <LevelState.h>
#include <LevelReader.h>
#include <AppLayer.h>

int main(int argc, char** argv) 
{
	sf::RenderWindow App(sf::VideoMode(800, 600, 32), "Escape the City - SFML", sf::Style::Titlebar);

	while (App.isOpen()) 
	{

		//start app layer

		//recieve level and initialize Views

		//start game loop

	}



	return 0;
}